/**
 * Allocation View
 *
 * Main view component for allocation domain.
 */

import { AllocationViewProps } from "./types";

export function AllocationView({}: AllocationViewProps) {
  return (
    <div>
      <h1>Allocation View</h1>
      <p>This is a generated view component for the allocation domain.</p>
      {/* TODO: Implement view */}
    </div>
  );
}
