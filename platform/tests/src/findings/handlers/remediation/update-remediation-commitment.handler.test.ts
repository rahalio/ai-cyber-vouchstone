/**
 * UpdateRemediationCommitment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: findings
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateRemediationCommitment } from "@vouchstone/api-server/domains/findings/handlers";
import type { FindingsDDDDependencies } from "@vouchstone/api-server/domains/findings";
import { createRemediation as createRemediationEntity } from "@vouchstone/factories/findings";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@vouchstone/tests/shared";
import { FINDINGS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateRemediationCommitment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: FindingsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<FindingsDDDDependencies>(
      FINDINGS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createRemediationEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { findingId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateRemediationCommitment, "execute").mockResolvedValue(useCaseResult);

    await updateRemediationCommitment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateRemediationCommitment.execute).toHaveBeenCalledWith(expect.objectContaining({ findingId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
