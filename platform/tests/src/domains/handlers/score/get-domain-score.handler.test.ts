/**
 * GetDomainScore handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: domains
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getDomainScore } from "@vouchstone/api-server/domains/domains/handlers";
import type { DomainsDDDDependencies } from "@vouchstone/api-server/domains/domains";
import { createScore as createScoreEntity } from "@vouchstone/factories/domains";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@vouchstone/tests/shared";
import { DOMAINS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetDomainScore", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DomainsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DomainsDDDDependencies>(
      DOMAINS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createScoreEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetDomainScore, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { domainKey: entity.id };

    await getDomainScore(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetDomainScore.execute).toHaveBeenCalledWith(expect.objectContaining({ domainKey: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { domainKey: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetDomainScore, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getDomainScore(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
