/**
 * ExecuteAttributeIncidentDiscovery - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: attribution
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAttributeIncidentDiscovery Use Case
 *
 * DDD: Application use case for get Attribution.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AttributeIncidentDiscoveryInput, AttributeIncidentDiscoveryOutput } from "../dto/attribution.dto";
import type { ExecutionContextService, IdGeneratorService } from "@vouchstone/services/_shared/index.js";
import type { AttributionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAttributeIncidentDiscovery {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly attribution: AttributionRepository
  ) {}

  async execute(input: AttributeIncidentDiscoveryInput): Promise<AttributeIncidentDiscoveryOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.atrId();

    // All field names extracted from OpenAPI spec: detectionSource, discoveredAt, discovererClass, estimatedCompromiseAt, recordedBy
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).incidentId) {
      throw new ValidationError("incidentId is required");
    }
    const incidentId = (input as any).incidentId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.attribution.attributeIncidentDiscovery({ incidentId: incidentId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${incidentId}`);
    }

    return entity as AttributeIncidentDiscoveryOutput;
  }
}
