/**
 * ExecuteScheduleEmulationExercise - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: exercises
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteScheduleEmulationExercise Use Case
 *
 * DDD: Application use case for get Exercise.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ScheduleEmulationExerciseInput, ScheduleEmulationExerciseOutput } from "../dto/exercise.dto";
import type { ExecutionContextService, IdGeneratorService } from "@vouchstone/services/_shared/index.js";
import type { ExerciseRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteScheduleEmulationExercise {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly exercise: ExerciseRepository
  ) {}

  async execute(input: ScheduleEmulationExerciseInput): Promise<ScheduleEmulationExerciseOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.exrId();

    // All field names extracted from OpenAPI spec: dueDate, providerIndependent, providerName, scenario, scopeAssetIds
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.exercise.scheduleEmulationExercise({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as ScheduleEmulationExerciseOutput;
  }
}
